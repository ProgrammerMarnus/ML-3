"""Dashboard module for Market Predictor ML.

Note: ``dashboard/app.py`` is a Streamlit entrypoint - import it via
``streamlit run market_predictor_ml/dashboard/app.py``, not through this
package init (to avoid requiring streamlit on plain library import).
"""
